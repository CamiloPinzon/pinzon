export const selectCategories = (state) => state.pills.categories;

export const selectSelectedCategory = (state) => state.pills.selected_category;

export const selectSelectedPill = (state) => state.pills.selected_pill;
